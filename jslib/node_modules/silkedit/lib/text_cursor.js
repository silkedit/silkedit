'use strict';

// used only by jsdoc

/**
 * [QTextCursor]{@link http://doc.qt.io/qt-5/qTextCursor.html}に対応するクラス。
 * @memberof module:silkedit
 */
class TextCursor {
  /**
   * @constructor
   */
  constructor() {}
  
  /**
   * @returns {module:silkedit.TextBlock}
   */
  block(){}
  
  /**
   * @param {module:silkedit.TextCursor.MoveOperation} operation
   * @param {module:silkedit.TextCursor.MoveMode} [mode=MoveAnchor]
   * @param {number} [n=1]
   * @returns {boolean}
   */
  movePosition(){}

  /**
   * @returns {number}
   */
  position(){}
  
  /**
   * @param {number} pos
   * @param {module:silkedit.TextCursor.MoveMode} [m=MoveAnchor]
   */
  setPosition(){}

  /**
   * @returns {string}
   */
  selectedText(){}
  
  /**
   * @param {string}
   */
  insertText(text){}
  
  /**
   *
   */
  removeSelectedText(){}
  
  /**
   */
  clearSelection(){}
}

/**
 * {@link http://doc.qt.io/qt-5/qTextCursor.html#MoveMode-enum}
 * @readonly
 * @enum {number}
 * @memberof module:silkedit.TextCursor
 */
const MoveMode = { MoveAnchor: Symbol(), KeepAnchor: Symbol() };


/**
 * {@link http://doc.qt.io/qt-5/qTextCursor.html#MoveOperation-enum}
 * @readonly
 * @enum {number}
 * @memberof module:silkedit.TextCursor
 */
const MoveOperation = {    NoMove,

    Start,
    Up,
    StartOfLine,
    StartOfBlock,
    StartOfWord,
    PreviousBlock,
    PreviousCharacter,
    PreviousWord,
    Left,
    WordLeft,

    End,
    Down,
    EndOfLine,
    EndOfWord,
    EndOfBlock,
    NextBlock,
    NextCharacter,
    NextWord,
    Right,
    WordRight,

    NextCell,
    PreviousCell,
    NextRow,
    PreviousRow
};

/**
 * {@link http://doc.qt.io/qt-5/qTextCursor.html#SelectionType-enum}
 * @readonly
 * @enum {number}
 * @memberof module:silkedit.TextCursor
 */
const SelectionType = {  WordUnderCursor, LineUnderCursor, BlockUnderCursor, Document };