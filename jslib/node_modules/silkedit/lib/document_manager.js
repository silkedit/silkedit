'use strict';

const bridge = process.binding('silkeditbridge');

// used only by jsdoc

/**
 * ドキュメントを管理するオブジェクト。
 * @namespace
 * @memberof module:silkedit
 */
const DocumentManager = {
  /**
   * ドキュメントを開く。
   * @function
   * @param {string} filename
   */
  open: bridge.DocumentManager.open
};
 
module.exports = DocumentManager;