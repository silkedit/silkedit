'use strict';

// used only by jsdoc

/**
 * keymap.ymlで使用される条件を扱うオブジェクト。
 * @namespace
 * @memberof module:silkedit
 */
const ConditionManager = {
  /**
   * 条件を追加する。
   * @function
   * @param {string} key - 条件キー
   * @param {object} cond - value()かisSatisfied(operator, operand)メソッドを持つオブジェクト
   * @param {module:silkedit.ConditionManager.value} cond.value
   * @param {module:silkedit.ConditionManager.isSatisfied} cond.isSatisfied
   */
  add: (key, cond) => {},

  /**
   * 条件を削除する。
   * @function
   * @param {string} key - 条件キー
   */
  remove: (key) => {}
};
 
module.exports = ConditionManager;

/**
 * 値を返す関数。返された値がkeymap.yml等の'if'の中で使用される。
 * @callback ConditionManager.value
 * @memberof module:silkedit
 * @returns {*}
 */

/**
 * keymap.yml等の'if'の中で使用される関数。
 * @callback ConditionManager.isSatisfied
 * @memberof module:silkedit
 * @param {string} operator
 * @param {*} operand
 * @returns {boolean}
 */