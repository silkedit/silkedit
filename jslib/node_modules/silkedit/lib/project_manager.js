'use strict';

const bridge = process.binding('silkeditbridge');

// used only by jsdoc

/**
 * プロジェクトを管理するオブジェクト。
 * @namespace
 * @memberof module:silkedit
 */
const ProjectManager = {
  /**
   * ディレクトリをプロジェクトとして開く。
   * @function
   * @param {string} directoryPath
   */
  open: bridge.ProjectManager.open
};
 
module.exports = ProjectManager;