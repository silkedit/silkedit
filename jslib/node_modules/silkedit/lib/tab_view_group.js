'use strict';

// used only by jsdoc

/**
 * [QWidget]{@link http://doc.qt.io/qt-5/qWidget.html}に対応するクラス。
 * @memberof module:silkedit
 */
class TabViewGroup {
  /**
   * newできない。
   */
  constructor(parent = null) {}

  /**
   * 新規[TabView]{@link module:silkedit.TabView}を追加する。
   */
  addNewTabView(){};
  
  /**
   * 既存の{@link module:silkedit.TextEdit}を水平に分割する。
   */
  splitHorizontally(){};

  /**
   * 既存の{@link module:silkedit.TextEdit}を垂直に分割する。
   */
  splitVertically(){};
  /**
   * 水平方向に分割して新規に[TabView]{@link module:silkedit.TabView}を作る。
   * @param {object} initialWidget - [QWidget]{@link http://doc.qt.io/qt-5/qWidget.html}に対応するオブジェクト。親は新規[TabView]{@link module:silkedit.TabView}となる。
   * @param {string} label - タブのラベル
   */
  splitHorizontally(initialWidget, label){};
  /**
   * 垂直方向に分割して新規に[TabView]{@link module:silkedit.TabView}を作る。
   * @param {object} initialWidget - [QWidget]{@link http://doc.qt.io/qt-5/qWidget.html}に対応するオブジェクト。親は新規[TabView]{@link module:silkedit.TabView}となる。
   * @param {string} label - タブのラベル
   */
  splitVertically(initialWidget, label){};
  /**
   * @type {module:silkedit.TabView[]}
   */
  get tabViews() {};
}