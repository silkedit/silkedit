'use strict';

const bridge = process.binding('silkeditbridge');

// used only by jsdoc

/**
 * 定数を持つオブジェクト。
 * @namespace
 * @memberof module:silkedit
 */
const Constants = {
  /**
   * パッケージを管理するpackages.jsonへのパス。
   * $HOME/.silk/packages/packages.json
   * @type {string}
   */
  userPackagesJsonPath: bridge.Constants.userPackagesJsonPath,
  /**
   * パッケージがインストールされているnode_modulesディレクトリへのパス。
   * $HOME/.silk/packages/node_modules
   * @type {string}
   */
  userPackagesNodeModulesPath: bridge.Constants.userPackagesNodeModulesPath,
};
 
module.exports = Constants;