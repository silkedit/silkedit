# common keymap

- { key: 'ctrl+w, s', command: split_horizontally, if: text_edit_focus }
- { key: 'ctrl+w, v', command: split_vertically, if: text_edit_focus }
- { key: ctrl+shift+p, command: show_scope, if: text_edit_focus }
- { key: ctrl+space, command: complete, if: text_edit_focus }
- { key: tab, command: indent, if: text_edit_focus }
- { key: 'ctrl+`', command: show_console, if: console_visible == false}
- { key: 'ctrl+`', command: hide_console, if: console_visible }

# keymap for Mac

- { key: cmd+n, command: new_file, if: onMac }
- { key: cmd+o, command: open, if: onMac }
- { key: shift+cmd+t, command: reopen_last_closed_file, if: onMac }
- { key: opt+cmd+s, command: save_all, if: onMac }
- { key: ctrl+b, command: move_cursor_left, if: onMac && text_edit_focus }
- { key: ctrl+f, command: move_cursor_right, if: onMac  && text_edit_focus }
- { key: ctrl+p, command: move_cursor_up, if: onMac && text_edit_focus }
- { key: ctrl+n, command: move_cursor_down, if: onMac && text_edit_focus }
- { key: cmd+s, command: save, if: onMac && text_edit_focus }
- { key: shift+cmd+s, command: save_as, if: onMac && text_edit_focus }
- { key: cmd+w, command: close_tab, if: onMac && text_edit_focus }
- { key: cmd+z, command: undo, if: onMac && text_edit_focus }
- { key: cmd+shift+z, command: redo, if: onMac && text_edit_focus }
- { key: cmd+x, command: cut, if: onMac && text_edit_focus }
- { key: cmd+c, command: copy, if: onMac && text_edit_focus }
- { key: cmd+v, command: paste, if: onMac && text_edit_focus }
- { key: cmd+a, command: select_all, if: onMac && text_edit_focus }
- { key: cmd+f, command: find_and_replace, if: onMac && text_edit_focus }
- { key: return, command: newline, if: onMac && text_edit_focus }
- { key: 'cmd+shift+]', command: select_next_tab, if: onMac && text_edit_focus }
- { key: 'cmd+shift+[', command: select_previous_tab, if: onMac && text_edit_focus }

# keymap for Windows

- { key: ctrl+n, command: new_file, if: onWindows }
- { key: ctrl+o, command: open, if: onWindows }
- { key: shift+ctrl+t, command: reopen_last_closed_file, if: onWindows }
- { key: alt+ctrl+s, command: save_all, if: onWindows }
- { key: ctrl+s, command: save, if: onWindows && text_edit_focus }
- { key: shift+ctrl+s, command: save_as, if: onWindows && text_edit_focus }
- { key: ctrl+w, command: close_tab, if: onWindows && text_edit_focus }
- { key: ctrl+z, command: undo, if: onWindows && text_edit_focus }
- { key: ctrl+shift+z, command: redo, if: onWindows && text_edit_focus }
- { key: ctrl+x, command: cut, if: onWindows && text_edit_focus }
- { key: ctrl+c, command: copy, if: onWindows && text_edit_focus }
- { key: ctrl+v, command: paste, if: onWindows && text_edit_focus }
- { key: ctrl+a, command: select_all, if: onWindows && text_edit_focus }
- { key: ctrl+f, command: find_and_replace, if: onWindows && text_edit_focus }
- { key: enter, command: newline, if: onWindows && text_edit_focus }
- { key: ctrl+tab, command: select_next_tab, if: onWindows && text_edit_focus }
- { key: ctrl+shift+tab, command: select_previous_tab, if: onWindows && text_edit_focus }