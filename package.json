{
  "name": "silkedit",
  "version": "1.0.0",
  "scripts": {
    "minify": "cmake -E remove_directory minified && cmake -E copy_directory jslib minified/jslib && babel minified/jslib -d minified/jslib --ignore resources && grunt"
  },
  "devDependencies": {
    "babel-cli": "^6.0.0",
    "babel-preset-es2015": "^6.5.0",
    "grunt": "^0.4.5",
    "grunt-cli": "^0.1.13",
    "grunt-contrib-uglify": "^0.11.1"
  }
}
