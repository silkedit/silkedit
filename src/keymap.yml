# Normal mode keymap

- context: mode == normal
  keymap:
    i: { cmd: change_mode, args: { mode: insert }}

# Insert mode keymap

- context: mode == insert
  keymap:
    p: { cmd: change_mode, args: { mode: normal }}
